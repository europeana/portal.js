const defaults = {
  short: {
    year: 'numeric',
    month: 'long',
    day: 'numeric'
  }
};

export default {
  bg: defaults,
  cs: defaults,
  da: defaults,
  de: defaults,
  el: defaults,
  en: defaults,
  es: defaults,
  et: defaults,
  eu: defaults,
  fi: defaults,
  fr: defaults,
  ga: defaults,
  hr: defaults,
  hu: defaults,
  it: defaults,
  lt: defaults,
  lv: defaults,
  mt: defaults,
  nl: defaults,
  pl: defaults,
  pt: defaults,
  ro: defaults,
  sk: defaults,
  sl: defaults,
  sv: defaults
};
