query CollectionTopicsPage(
  $identifier: String!,
  $locale: String!,
  $preview: Boolean = false
) {
  collectionTopicsPage(preview: $preview, locale: $locale, where: { identifier: $identifier }, limit: 1) {
    total
    items {
      identifier
      name
      description
    }
  }
}
