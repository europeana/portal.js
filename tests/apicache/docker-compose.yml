version: "3"
services:
  server:
    image: portaljs-apicache-server
    build:
      context: ./server
    expose:
      - "80"
  gateway:
    image: portaljs-apicache-gateway
    build:
      context: ./gateway
    depends_on:
      - server
    ports:
      - "4433:443"
