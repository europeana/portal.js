version: "3"
services:
  server:
    image: europeana/portaljs-apicache-server
    # build:
    #   context: ./server
    expose:
      - "80"
  gateway:
    image: europeana/portaljs-apicache-gateway
    # build:
    #   context: ./gateway
    depends_on:
      - server
    ports:
      - "4433:443"
