# This is a template .env file for CI environments, whitelisting variables to
# include when running the app via the Docker stack, populated from the CI's own
# environment variables with `envsubst`.

APP_IMAGE_VERSION=${APP_IMAGE_VERSION}
CTF_CDA_ACCESS_TOKEN=${CTF_CDA_ACCESS_TOKEN}
CTF_ENVIRONMENT_ID=${CTF_ENVIRONMENT_ID}
CTF_GRAPHQL_ORIGIN=${CTF_GRAPHQL_ORIGIN}
CTF_SPACE_ID=${CTF_SPACE_ID}
ENABLE_JIRA_SERVICE_DESK_FEEDBACK_FORM=${ENABLE_JIRA_SERVICE_DESK_FEEDBACK_FORM}
EUROPEANA_API_KEY=${EUROPEANA_API_KEY}
OAUTH_CLIENT=${OAUTH_CLIENT}
PERCY_TOKEN=${PERCY_TOKEN}
PERCY_BRANCH=${PERCY_BRANCH}
PERCY_COMMIT=${PERCY_COMMIT}
PERCY_PULL_REQUEST=${PERCY_PULL_REQUEST}
