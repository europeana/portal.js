query CategorySuggest(
  $locale: String!,
  $text: String!
) {
  categoryCollection(
    preview: false,
    locale: $locale,
    where: { name_contains: $text },
    limit: 10
  ) {
    items {
      name
      sys {
        id
      }
    }
  }
}
