query CategoryFind(
  $locale: String!,
  $ids: [String]!
) {
  categoryCollection(
    preview: false,
    locale: $locale,
    where: { sys: { id_in: $ids } },
    limit: 10
  ) {
    items {
      name
      sys {
        id
      }
    }
  }
}
